# Optional extras for the NOVA Python backend.
# The core runtime (nova_runtime) is stdlib-only; install these only when you
# want OCR (pillow + pytesseract + a system Tesseract binary) or desktop
# automation (pyautogui).
pillow>=10.0.0
pytesseract>=0.3.10
pyautogui>=0.9.54
faster-whisper>=1.2.1
